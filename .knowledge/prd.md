# OpenClaw管理器

## 概述

1. 一个OpenClaw（龙虾）远程管理器，可以远程管理龙虾主机
2. 支持创建、编辑、删除等管理管理操作
3. 支持远程连接OpenClaw
4. 完全独立于OpenClaw，启动一个独立的代理服务与OpenClaw的ws通道进行交互，启动时可以自动探测是否有OpenClaw服务存在并自动连接
5. 支持管理OpenClaw的配置文件，通过OpenClaw 的cli工具管理和配置OpenClaw
